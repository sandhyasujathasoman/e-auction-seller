server:
  port: 5050
  servlet-path: /

spring:
  application:
    name: e-auction-seller-service
  data:
    mongodb:
      uri: mongodb://root:password@docdb-2022-07-20-11-58-08.cluster-chyaujt2bxdh.us-east-1.docdb.amazonaws.com:27017/?ssl=true&ssl_ca_certs=rds-combined-ca-bundle.pem&replicaSet=rs0&readPreference=secondaryPreferred&retryWrites=false

info:
  app:
    name: ${spring.application.name}
  build:
    version: ${project.version}

app:
  clients:
    web-client:
      host: http://localhost:3000
  services:
    buyer-service:
      scheme: http
      host: localhost
      port: 6060
      bid-search: /e-auction/api/v1/buyer/show-bids/{productId}
